name = "affimark-backend"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
NODE_ENV = "development"

[dev]
port = 8787
local_protocol = "http"

# Browser Rendering for Puppeteer-based scraping
[browser]
binding = "BROWSER"
